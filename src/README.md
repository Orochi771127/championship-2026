# Source boundary

Standalone Championship 2026 runtime code is added by the VS1 migration checkpoint. This tree may depend on local npm packages and product assets only; it must not resolve a Nexus Link or `research/` path.

