# 原作名單資料填入 — 2026-09-13

ROM SHA-256 `8ad375ba0bd9b652a25f72dead2b47f78da401e188a8f3e1b7a6f2867ee0c5d1`。
使用 ndspy、Capstone、Ghidra／pyghidra-mcp；原作函式與格式表直接核對，未以欄位名稱猜測記憶體。

`02088128` 引用 `ui/digimon_list_sub.nxr`；`02088240` 透過 `020CD9B4` 的 17 個依序節點索引綁定文字。
`02088480` 將個體指標存入 view + `0x17C8`，填入從 `view + 0x1384` 開始、間隔 `0x40` 的 17 組文字；`02088830` 將這些文字送入同一組節點。
節點身分與 Owner 美術包 `NXR_NODE_TABLE_1369.csv` 完全一致。

| 節點 | 個體來源 | 原作格式 | 本批 |
|---|---|---|---|
| HP | +050 / +058 | 目前值／最大值 | 接回名單 |
| TP | +054 / +05C | 目前值／最大值 | 接回名單 |
| size | +010 | 整數 G | 接回名單 |
| attack / def / wiz / spd | +060 / +064 / +068 / +06C | 整數 | 接回名單 |
| sp1 / sp2 | +12C / +130，加 0x4FC 查文字 | 技能名稱 | 核對原作 31 個名稱並接入繁中 |
| num_battle | +024 | 整數 | 接回名單 |
| win_rate | +028 / +024 的 Q12 計算，轉浮點除 4096 | 一位小數百分比 | 原作算術與浮點指令 50 組重跑通過；含 0 場顯示 0.0 |
| family / gen | 物種表 +018 / +00C 對應文字 | 分類名稱 | 接回原文對應繁中；family 取最低有效 bit 的次序 |
| mind | 個體 +018；蛋改用文字 0x1CC | 性格名稱 | 核對 9 名稱並接入繁中；蛋保留 ？？？ |
| digitama | 個體 +03C 的 signed byte | 整數 | 接回名單 |
| nickname | 個體 +02C 名稱緩衝 | 自訂名稱 | 沿用正式名稱 authority，修正節點標示 |
| name | 物種 +0x21 查文字；蛋另用 0x1CD | 種類名稱 | 沿用既有翻譯 |

`ap` 是第 17 號動態區域；`0208890C` 畫五項抗性圖形，不是另一個可直接填零的文字欄位。完整圖形比例、排列及繪製仍另驗。

數字參數收據 `ROSTER_UI_FIELDS_CPU_2026-09-13.json`：228 種個體 × 11 欄。
勝率收據 `ROSTER_WIN_RATE_CPU_2026-09-13.json`：50 組，原作 ARM9 固定點除法與 soft-float 指令實跑，僅最後文字 formatter 與畫圖接點為 stub。格式字串為 `%.1f %`。因此數字來源／換算已驗，未宣稱原作字型或抗性圖已完全一致。馴獸師整數勝率不能拿來取代這一欄。

上述新資料只擴充既有個體資料的展示投影，不增加存檔欄位，不用物種預設值填補舊存檔未知資料。原作代碼、記憶體與圖像保留在外部研究工具區；本文件只保存來源關係。
