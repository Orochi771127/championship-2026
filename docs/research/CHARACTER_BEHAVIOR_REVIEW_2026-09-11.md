# Character behavior implementation and Claude review — 2026-09-11

This checkpoint continues `CHARACTER_BEHAVIOR_RESTORATION_STAGE.v1`; it does not close all original behavior. There are 228 species bindings sharing 224 resource sets (216 regular, eight egg). Resource coverage and helper tests do not establish normal-path acceptance for every species/action pair.

| Work | Verified contribution | Current boundary |
|---|---|---|
| Raising hand | Tap/stroke/hold classification, admission, sleeping stroke, stress/affection writes, tap reactions and release now use the existing actor/app/Pixi path. | Browser hatch/stroke/carry/landing/Save/Continue accepted; evolution-hint producer `actor+454`, sound playback and complete visual/device comparison remain open. |
| Claude championship rounds | Three-round and five-round cursor/win/result helpers with bounded CPU evidence. | Missing/future flags and continuation after loss now rejected. Full entry/schedule/round transitions/settlement are not connected to normal play. |
| Championship opponents | Original ARM9 initializer `020998D4` reconstructs 28 pool records from team table `020ED2D8`. The earlier live-BSS-only limitation is superseded. | Immutable catalog carries team indices; channel 0 selector matches 824 controlled CPU cases. This is not a playable tournament. |
| Claude tutorial | 35 text-indexed steps, three phases, Traditional Chinese display copy and cursor helpers. | Action names are inferred proposals, not traced cartridge predicates. Normal onboarding is unbound. Save publication and valid older cursors outside the new subset now preserve save integrity. |
| Claude Hunt steering | Original fall-through, normalization and blend-rate evidence; three palette cells retain the required rate. | One actor's scratch heap address is not encounter truth. Removed that constant fallback. Captured replay can supply its own words; missing source state interrupts play visibly and allows exit with completed card entries. Exact steering remains unknown. |

Claude's 9,216-case cone is sampled evidence, not execution over every heap address or identical-position proof. Historical probes and handoff remain available, with their completion language superseded here.

## Source and implementation

`scripts/research/check-raising-hand-cpu.py` verifies the local YDIJ ROM hash and executes ARM9/OVL18 with Unicorn. `RAISING_HAND_CPU_2026-09-10.json` records 210 classifier cases, 40 stroke-controller cases, 1,920 stroke updates over 32 profiles, 288 sleep-command cases, 20 tap entries and 1,536 tap-reaction cases. Actor, hit/admission and projection inputs are controlled; observed sound/sequence requests do not prove playback.

`nativeRaisingHand.js` and the existing actor/growth/RNG/save owners consume the results. Native pixel motion boundaries and four-update pickup replace the browser hold timer. Blur, cancellation, scene change, resize, context loss, New Game, Continue and day transitions release transient ownership. Readonly developer hit-center diagnostics use the existing Pixi update without a new ticker/global game API. Tests additionally cover 220 regular species bindings with eight condition masks (1,760 cases), not human playthroughs.

`scripts/research/check-championship-pools-cpu.py` runs original initialization without hooks from BSS bytes seeded `0xA5`, matching all 28 complete 20-byte records to the original team table. The original selector prefix uses controlled category/round/RNG inputs and stops before individual construction. `CHAMPIONSHIP_POOLS_CPU_2026-09-10.json` records 824 comparisons. Runtime carries consumed indices, not ROM bytes or emulator memory.

## Acceptance and open scope

Browser outputs now stage under `.tmp/browser-qa`; `CHAMPIONSHIP_QA_OUTPUT_ROOT` permits deliberate exports without ordinary tests overwriting historical reports. Raising acceptance uses rendered hit centers and the actual egg-to-adult hand flow. Hunt diagnostics previously lagged the Pixi actor because they updated with DOM publication; they now follow each existing render. The capture gate also fixes the initial browser Date used by original RNG and waits for a live native control-frame increment for every pointer sample and follows the actual native overlap-selected rope target and restores low rope durability using ordinary held slack before resuming a strong pull. Earlier source-only successes and the failed artifact run were insufficient; final source and artifact runs are recorded separately. See [current debt](../TECH_DEBT_REGISTER.md) and [final checkpoint validation](../reports/character-behavior-2026-09-11/VALIDATION.json).

Full restoration still requires Raising hint/audio/peer ordering, unresolved Hunt producers/branches, normal tournament flow, tutorial predicates and binding, battle per-move caller/ending timing, all-species normal-path/visual comparisons and physical-device acceptance. Publication of this checkpoint does not close these obligations.
