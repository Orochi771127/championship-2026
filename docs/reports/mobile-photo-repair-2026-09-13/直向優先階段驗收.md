# 直向優先：本機階段驗收 — 2026-09-13

## 範圍與結論

依 Owner 最新選擇完成第一種方案：手機橫置時，以同一個 9:16 直向介面置中，兩側留空。完整橫向介面另行討論。本階段的實作與本機瀏覽器驗收完成；不表示原先所有截圖問題、全部原作功能或真實 iPhone Safari 驗收完成。

工作位置 `R:/Projects/Championship2026/championship-2026`，分支 `main`，基準 HEAD `96885deb42910a0fa63674a31c536768fc60d19c`。延續同一工作樹的上一輪照片修復，沒有重置或覆寫其他 Agent 的工作。整合結果保留為本機 checkpoint；沒有 push、merge 或 deploy，公開網站尚未取得本批修正。

## 實際修正

- 橫置維持 390 × 693.33 的邏輯直向版面，依可見區域等比例縮放置中。上方狀態列、畫面、工具列一起縮放。
- 使用可見視窗的大小及位移處理瀏覽器工具列與鍵盤；使用者捏合放大時不做反向縮小。
- 輸入暱稱造成高度縮短時，保留直向判定，並把輸入欄位捲入可見區域；真正旋轉仍正常切換。輸入文字保留。
- Pixi 使用未縮放的版面尺寸，沿用既有畫布事件換算；狀態列與工具列也改用未縮放高度預留空間，避免縮放兩次。
- 舊的短視窗壓縮規則限於真正的直向短視窗；橫置使用對應 693.33 高度的直向配置。沒有新增 router、store、Save authority、Pixi bootstrap 或 ticker。
- 本階段只調整呈現與視窗處理，未重畫角色、改動原作玩法或更換資產來源。上一輪的循環場地、邊緣拖曳與照片修復詳見 [上一輪紀錄](驗收與交接.md)。

## 瀏覽器驗收

使用 Codex Browser 外掛與既有 QA fixtures，獨立測試 origin `http://127.0.0.1:8750`；未操作使用者公開網站的存檔。使用 frontend-testing-debugging、game-ui-ux 與既有 Pixi/美術邊界 Skills。育成及狩獵場景診斷使用 developer presentation；捕獲清單為受控 fixture，不冒稱當場正常捕獲 17 隻。

| 檢查 | 結果 |
| --- | --- |
| 320 × 568、390 × 693、393 × 700 直向 | 保留直向版面；商店、育成與狩獵實際操作 |
| 568 × 320、667 × 375、844 × 393 橫向 | 同一個 9:16 框置中，四邊在可見區域內 |
| 育成 390 × 693 ↔ 844 × 393 | 最終兩邊畫布內部尺寸均為 387 × 342；狀態列預留 35px、工具列 60px；可點選角色 |
| 商店、選場、完整裝備清單 | 橫置可選分類／商品、選地點、捲動選裝備並進場 |
| 六類狩獵工具與兩個外掛 | 橫置六工具同一排，雷達與目標框分開；選取繩索後再次橫轉，選取保留 |
| 對戰選單／六隻參賽名單 | 本輪走既有選單及名單，列表可捲動到返回操作；四模式未接線並未因此完成 |
| 暱稱輸入，393 × 700 → 393 × 280 | 保持直向；欄位可見，不被當成手機旋轉 |
| 暱稱輸入，再橫轉及轉回 | 名稱保留，框與輸入位置正常更新 |

自動測試另覆蓋 1024 × 768、1920 × 1080、大視窗置中、可見視窗位移、鍵盤關閉先後順序、捏合放大與監聽器清理。自動模擬並非真實手機驗收。

截圖及量測保留於 `C:/Users/USER/.codex/visualizations/2026/09/13/01a0994f-6ee1-7151-851c-88f0d268eb6e/`，使用 `portrait-stage-` 前綴。原始量測包含修正前後過程，最終紀錄以 `final` 標記區分。

## 最終檢查

- `npm test`：1,512 / 1,512 通過。
- `npm run test:ci`：1,295 / 1,295 通過。
- `npm run validate:preload`：180 個啟動模組，通過。
- `npm run build:playtest`：6,285 個檔案；buildId `0428338cdba276d90f743423a93e96d52f4df32c9adcdf4a8916061da73b1496`。
- `npm run validate:playtest`：通過，檔案數與 buildId 同上。
- `git diff --check`：通過；協作狀態與 CI 分類 JSON 解析通過。

## 明確未關閉的項目

1. 真實 iPhone Safari 的地址列收合、鍵盤與旋轉操作，仍需實機確認。
2. 商店 79 筆逐商品正式圖示對應未完成；本批沒有用共用圖片冒充。
3. 四面對戰模式的完整入口、資格、賽程接線仍未完成。
4. 籠子地面與各大型角色腳底／圖面完整對齊仍為部分完成；預留上方空間不代表全部站位驗收通過。

後續安全工作是逐項查證並閉合以上原作還原缺口；另做完整橫向介面需再討論設計。`fullOriginalParity=false`、`physicalPhoneQA=false`、`publicationPerformed=false`。
