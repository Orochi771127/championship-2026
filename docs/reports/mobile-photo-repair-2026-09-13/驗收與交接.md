# 手機照片問題修復與驗收 — 2026-09-13

此文件保留第一輪修復當時的測試與版本狀態；同日後續的直向穩定化、最終整合檢查與本機 checkpoint，請見 [直向優先階段驗收](直向優先階段驗收.md)。

本次依 Owner 提供的 9 張直向照片、7 張橫向照片及補充操作需求修復。最新方向是直向優先：橫置暫時把同一個 9:16 版面置中，兩側留空，完整橫向配置另行討論。

開工位置：`R:/Projects/Championship2026/championship-2026`；`main`；基準 `96885deb42910a0fa63674a31c536768fc60d19c`；起始工作樹乾淨。本批沒有 commit、push、merge、deploy、rebase 或 reset。原網站尚未取得這批修正。

## 本批結果

| 問題 | 處理與實際驗收 | 狀態 |
| --- | --- | --- |
| 橫放各頁被壓扁、裁切 | 同一個 390 × 693.33 邏輯直向框整體縮放置中；撤下小高度橫向重排。Pixi 使用未縮放的排版尺寸，指標由畫布顯示矩形換算。844 × 393 下檢查育成、狩獵、商店及標題。 | 本機通過；真實 Safari 仍待驗 |
| 對戰顯示場地載入失敗 | 音效網址不再清掉 `/championship-2026/`。選用音效失敗不再使整個場地掛載失敗；原錯誤留在 console。 | 路徑回歸與一場正式入口流程通過 |
| 參賽名單與底部按鈕重疊 | 清單列保留內容高度，外層可捲動，取消把整份名單壓進短框。 | 六隻完整 QA 名單及有效單人隊伍通過 |
| 狩獵道具擠疊 | 分類框改成標題在上、雙欄選項在下；名稱／數量分行；框外、項目之間留空。 | 完整庫存可捲動、選取與進場 |
| 狩獵工具散成兩排 | 保持既有順序：手、繩索、射擊、鋼索、誘引道具、傷害陷阱；同一橫排。 | 六類裝備實際進場，各工具 Y 座標同為 572 |
| 雷達壓住目標資料 | 雷達／記憶卡資訊與目標框放入同一排的兩個區塊。 | 成熟期雷達與完整分析同時裝備通過 |
| 捕獲名單只有一張圖 | 每一列依自身 speciesId 使用既有登記的 HUD 角色圖；名單、釋放及存檔權限沿用原流程。 | 受控 17 列全部圖片載入，可捲動查看 |
| 拿著數碼獸無法移動視野 | 拿起後停在左右邊緣，既有 ticker 持續移動相機並更新手的位置。放開、離開畫布後停止邊緣移動。 | 實際先拿起再拖到右緣，場地跟隨並放下 |
| 場地滑到底無法繼續 | 相機按現有原生地面週期循環；相鄰畫面共用原本紋理，不增加載入／卸載權限。 | 持續滑動可回到等待區；接縫正反轉換及資源清理測試通過 |
| 大型角色頂到上方面板 | 地面上方預留 64 原生像素的展示空間，角色與地面沿用同一縮放。 | 上方留白改善；完整腳底／圖面對齊仍 PARTIAL |
| 商店工具／外掛缺圖 | 確認仍有 79 筆尚無逐商品的正式圖示對應，本批沒有以共用圖示冒充個別原作圖片。 | 未完成 |
| 四面對戰模式入口未接 | 冠軍賽、頭銜賽、自由對戰、通訊對戰的選擇接線維持原狀；不把目前可用比賽冒稱四模式完成。 | 未完成 |

## 座標與素材邊界

- 原生地面週期仍為 `unlockedCount × 48` 原生像素。空置欄位仍存在，未壓縮配置、建立另一份地面或改動 Save。
- 接縫只在計算拖曳速度之前，把上一個位置表示成同一個週期副本；原有速度、拋出、碰撞、落地計算仍使用既有實作。新增實際 app 測試確認跨接縫的小位移不會變成整個場寬的投擲。
- 邊緣寬度及捲動速度是手機觸控呈現參數，不宣稱已逐幀還原原作相機速度。
- 原作地面 wrapping 證據：`docs/research/RAISING_FEEDING_STAGE_2026-09-08.md` 的 ARM9 `020502D8`、`0207C9E8`、`0207CC24`；既有 native ground、ranch startup、carry CPU 對照測試。
- 多角色結果名單：`docs/research/video-BV13u411B7BK/VIDEO_OBSERVATIONS.csv` V12，以及私有影片參考 `t072.png`。本批只使用既有正式路徑的角色圖，未把私有研究圖加入 runtime。
- 籠子計畫仍標示 `NATIVE_ORIGINS_AND_CROP_WITH_FLATTENED_ART`。地面座標與各籠子平面圖的完整對齊未閉合，不能把上方預留空間說成所有站位都修好。
- 四模式與商品缺圖沿用 `docs/reports/tooling-parity-audit-2026-09-13/分批修復進度.md` 的待查界線。

## 驗證

- 最終 `npm test`：**1504 / 1504**。
- 最終 `npm run test:ci`：**1287 / 1287**。
- `npm run validate:preload`：179 個啟動模組，通過。
- `npm run build:playtest` → `npm run validate:playtest`：依序通過，6284 個檔案；buildId `ca423aa2bc6c8a1ea81dbef12041b7be80f725a7d6460ad34cb8938648374096`。
- `git diff --check`：通過。
- 本機瀏覽器外掛：獨立 `127.0.0.1:8749` origin。完整庫存使用既有 full QA fixture；有效比賽使用既有成年隊伍 fixture 後，走正式遊戲選單，不修改使用者公網存檔。
- 對戰完整路徑：存取突擊兵 → 合資格亞古獸 → 決定 → 一般場地 → 雙方 HP 變化 → 對手獲勝 → 獎金 → 戰績 → 返回牧場；觀察期間 console error/warn 為 0。
- 捕獲名單驗收是受控收納 fixture；本次沒有宣稱正常狩獵捕獲 17 隻。
- Skills／外掛：frontend-testing-debugging、game-ui-ux、pixijs-events、championship-art-production；Codex Browser；Championship Evidence MCP。

## 檔案與後續入口

主要修改為既有 `main.js`、`nativeUiSkin.css`、`shopOriginalVideo.css`、`vs2Screens.js`、`vs3Screens.js`、Pixi stage、Raising viewport／presentation／tile compositor、Battle audio。未新增 router、store、save authority 或 ticker。

後續仍需逐項閉合：79 筆商品的 item-index → cell/sequence → 正式素材；四面對戰入口的模式／資格／賽程對應；各種籠子地面與角色腳底的視覺對齊。以上未完成事項不能因本批測試通過而升格為原作完整一致或實機驗收通過。

本機截圖與原始測試紀錄存於 `C:/Users/USER/.codex/visualizations/2026/09/13/01a0994f-6ee1-7151-851c-88f0d268eb6e/`。這些是本機驗收資料，沒有加入部署檔案。
