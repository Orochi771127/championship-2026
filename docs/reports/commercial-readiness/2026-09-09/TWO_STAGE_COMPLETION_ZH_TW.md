# 剩餘工作的兩階段執行紀錄

Owner：「目前還沒做好的項目，請你最多兩個階段完成」。承接 main 74b264f；不增加第三階段，也不把部分完成改稱全部完成。兩階段之間持續工作，不另設開工確認。

## 第一階段：完整可玩流程與故障修正

| 工作 | 完成條件 | 狀態 |
| --- | --- | --- |
| 狩獵方向故障 | 原作對照、明確的例外政策、正常場景跨問題區域不崩潰及回歸 | 192 組 CPU 對照與正常邊緣案例完成；Owner 要求優先照原作，替代方向未套用，完整記憶體來源仍待閉合 |
| 自有隊伍與多輪大會 | 自有個體選擇、資格、正常參賽、逐輪推進、個體／獎勵回寫與存檔 | PARTIAL；頭銜賽自有個體選隊／資格／結算與存檔已接入，多輪大會仍 OPEN |
| 完整互動教學 | 新存檔從原作教學入口走完，續玩不重播、不遺失進度 | OPEN |
| 生命週期與晨間事件 | 缺少 writer 和形態配對逐項追證，事件順序／存檔重載一致 | PARTIAL |
| 畫面與存檔穩定性 | GPU 恢復、背景恢復、存檔衝突／復原使用原有權威，錯誤不破壞資料 | PARTIAL；GPU 恢復、分頁互斥、拒絕舊版覆寫與失敗進度匯出已接入，完整復原匯入／裝置驗收仍 OPEN |

第一階段驗收必須走正常新遊戲→育成→狩獵→返回→自有隊伍參賽→多輪結算→儲存／繼續；函式測試不能替代整條流程。

## 第二階段：正式內容與發行驗收

| 工作 | 完成條件 | 狀態 |
| --- | --- | --- |
| 正式美術、所有動作與音效 | 每項內容具來源、人工檢視、原作動作對照與正常 runtime 驗收 | OPEN |
| 裝置／長時間驗收 | 桌面與可用的實體手機完成正常流程、背景／恢復、效能與長時間測試 | OPEN；尚無實體手機驗收證據 |
| 可重建完整包 | 乾淨 checkout + 受控素材來源可重建、逐檔驗證、無私有素材外洩 | PARTIAL |
| 最終權利核驗 | Owner 提供的實際文件及核准範圍完成核驗；仍留在最後 | DEFERRED_UNTIL_RELEASE |
| 上架候選 | 依確定的平台完成包裝、必要商店資料與發行檢查，產生可審查候選包 | OPEN |

正式授權、平台帳號／簽署及實體手機驗收不能由程式測試代替；缺少時留在第二階段列為未完成，不捏造通過。本次指示授權完成工作，沒有新增公開部署授權。

## 本次進展

- 新增 [自有隊伍與存檔驗收紀錄](OWNED_PARTY_AND_SAVE_ZH_TW.md)：23,256 組資格 CPU 對照、216 組個體結算 CPU 對照、正常 DOM 選隊／實際戰鬥／存檔讀取及分頁交接。最新完整本機回歸 1,397 pass / 0 fail / 0 skip，101.71 秒。
- 共用 gameplay RNG 只在成功參賽時接入；被拒絕的準備不改亂數。支援 Web Locks 的環境只允許一個 active save session，Save & Quit 釋放；無 Web Locks 環境保留 stale-byte 防護的能力界線。

- 原作 MCP 查詢 0210D8B4 無對應索引結果後，使用既有 ROM hash-locked 研究工具追證 OVL0。
- 0210D9A8..0210DAD4 在低 nibble 12..15 不寫入目標方向 X/Y；0210DAE8 以後仍讀取該堆疊。新受控 CPU 工具固定原始狀態 SHA，檢查 192 組輸入；16 組未知方向／旗標配對全部受先前堆疊數值影響。這不是正常遊玩驗收。
- 證據：HUNT_STEERING_STACK_CPU_2026-09-09.json；工具 scripts/research/check-hunt-steering-stack-cpu.py。

- 追加正常 DeSmuME 回放：原先目標的捕獲 206 次方向計算及自由逃跑 364 次均未經未知分支；另一個地圖邊緣目標在開始逃跑的第一個更新實際讀取 0x8f。最後寫入來自動畫函式 02047D5C PUSH，內容為個體 actor 位址及 02047944 返回位址。這是實際來源，不代表所有進場的 heap 配置已重建。
- 修正逃跑起始方向 Y：原作 CPU 及既有正常回放均為 1，現代實作誤用 4096。6 項 movement tests 通過；仍不宣稱特殊堆疊方向故障已閉合。
- 同一 Pixi stage 已加入 context restored 處理，保留原 canvas／scene，恢復前重設 clock；2 項恢復測試及 9 項 clock 測試通過。正常瀏覽器驗收進行中。

- 完整本機回歸：1,388 pass / 0 fail / 0 skip，88.86 秒。
- 正常瀏覽器驗收：獨立 8765 origin，LOGIN→新遊戲→故事／命名→育成，使用 WebGL context loss extension 中斷／恢復；同一畫布恢復數碼蛋與場景，DOM 遊戲時間繼續前進。兩次實際畫面均已查看。未做真機驗收。瀏覽器紀錄有一筆較早、無來源網址的 MutationObserver 錯誤，以及預期的 context-loss 警告；不宣稱零錯誤。驗收分頁已關閉。

目前仍為第一階段 PARTIAL；這兩項有證據的修正，不等於自有隊伍、多輪大會、教學、正式美術或完整狩獵特殊分支已完成。第二階段保持 OPEN。
