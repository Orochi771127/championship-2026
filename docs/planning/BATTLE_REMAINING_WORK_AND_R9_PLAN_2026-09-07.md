# 對戰剩餘工作與下一階段 R9 計畫

**2026-09-08 接續更新：** 已補世界 +5EA8 bit1 正常接線、state16/actionId1 後續與 status1/state8追擊，25/25重點測試及1216/1216回歸通過。seed9現在驗證自然追擊，19/25驗證敵我復起後再行動。原始151組2D特效bank已定位，下一步接 R10 的第一組投射物完整生命週期與旋轉碰撞；下文舊有 action1／旗標未接敘述由 [本輪報告](../art/production/battle-normal-closure-r9/IMPLEMENTATION_2026-09-08.md) 取代。state16/action1自然全招式驗收及其他status body仍開放。

狀態：**R9 正常流程 slice 已實作及驗證；整體 R9／原作對戰仍 PARTIAL。** Owner 後續「開工」已授權並完成本輪正常站位、選目標、接近、三槽發動、命中／復起及連續出招的接線。完整回歸 1208/1208；三場正常 AI 結算記錄及手機尺寸檢查見 [R9 實作與證據邊界](../art/production/battle-normal-flow-r9/IMPLEMENTATION_2026-09-07.md)。下文保留原定完整驗收條件；整場原作 RNG／狀態 trace、state16 action1、profile 來源等未驗收項目不能因本輪通過就標為完成。

核對位置：`R:\Projects\Championship2026\championship-2026`，Git root 相同，branch `main`，HEAD `d0c48f340baac61cf399bf5bd5922ce58f3d38c7` 加目前未提交工作樹。保留共享修改。

## 已完成基線

- R8 已接通正常命中後的受擊資料寫入、擊退／落地、復起及最終倒下；成功復起能回到現有 AI 出招流程。
- 已有命中／狀態寫入 251 組、反應更新 8,106 幀、落地與狀態邊界 192 組、復起 1,254 組、持續傷害 245 組原始 CPU 比對。
- 上一輪集中測試 45/45，完整回歸 1195/1195。這次讀取既有紀錄，沒有把它列為新執行的測試。
- R8 只完成其合約內的核心流程。完整異常狀態行為、全招式正常流程、牧場自有組隊與實體手機／shipping 驗收仍未完成。

## 下一步：R9 原作正常攻擊流程

目標是先完成一個可用原作執行紀錄比較的正常對戰案例：

**原作初始站位 → 選目標 → 接近／轉向 → 檢查能否發動 → 配置攻擊物件 → 出招 → R8 命中／受擊 → 收招／中斷 → 再次行動。**

將這段排在完整投射物之前，是因為目標、位置、方向、發動時機與攻擊物件擁有者，都是投射物起點、碰撞及特效清除的前置資料。這是本次工程排序的調整，取代 R8 報告末尾「下一步先補投射物」的建議；R8 的已完成與未完成狀態不變。

R9 第一個正常案例使用既有可重現的預設隊伍與場次，並明示測試輸入。優先檢查目前正常場次已會使用的 move179，再由 ROM 確認它所走的近身流程；不使用強制命中或直接改 HP 代替正常選招。其餘已知 moves254/98 作為既有場次回歸參照，不能因同一場次出現就假定屬於同一招式家族。

| 執行順序 | 具體工作 | 完成條件 |
| --- | --- | --- |
| R9-A：補證據與欄位對照 | 追蹤初始站位的實際寫入點、選目標入口、接近與發動的呼叫鏈、C144 所有可達拒絕分支；核對舊報告的名稱與新資料是否一致 | 每個要接入的條件有 ROM 位址、輸入、輸出與觸發案例；缺少的來源列為 UNKNOWN_REQUIRES_TRACE，不能用目前遊戲畫面反推原作 |
| R9-B：接初始站位、目標與移動 | 在既有角色／世界座標中接上所選場次的原始站位、目標偏好與失效重選、接近／轉向；重用已驗證的定點數運算 | 同樣輸入與 RNG 歷史下，目標、位置／方向及狀態切換與原始執行紀錄一致；畫面與碰撞讀同一份座標 |
| R9-C：接發動與收招 | 接回該招式的原始狀態路徑、C144 拒絕與資源扣款順序，以及原作三個一般攻擊槽；串接主／輔助 VM 與取消／釋放 | 拒絕、配置失敗、資源不足、目標失效時，副作用依 ROM；每次成功發動只扣應扣資源；多個在途攻擊的擁有者不互相覆寫 |
| R9-D：接 R8 並驗收 | 以正常 AI 完成命中、未接觸、受擊中斷、目標倒下、復起後再出招、正常結算 | 與 ROM 比對本輪涵蓋的分支，攻擊、動畫與碰撞時機一致；結算無殘留物件；R8 回歸不退步；手機尺寸瀏覽器有可查看紀錄 |

R9-A 的第一個工程動作：**產生正常攻擊從站位初始化到收招的原始執行紀錄，列出現有 adapter 與 ROM 的差異，再改對應的既有函式。** 已經有來源的邏輯直接沿用，不重做已驗證模組。

對接位置：

- `src/championship/app/battleRuntime.js`：目前以既有 adapter 挑選目標、配置攻擊與準備發動。
- `src/championship/battle/battleSession.js`：目前 runnable states 與通知更新；R8 復起後仍回到部分完成的普通攻擊接線。
- `src/championship/battle/battleTurnStates.js`：已有等待、拒絕、三槽與部分發動規則，須核對後接到正常路徑。
- `src/championship/battle/battleLaunchPool.js`、`battleNativeLaunch.js`、`battleNativeActors.js`：沿用物件配置、擁有者、VM、動畫與座標。
- `src/championship/app/battlePresentationSource.js`：目前 `battleStandPosition` 仍標記 PRODUCT_AUTHORED，R9 須由已確認的初始化資料取代本輪涵蓋場次的位置。
- `src/championship/battle/battleHitState.js`、`battleHitRuntime.js`：沿用 R8 受擊／復起，補三槽中斷所需的接線。

R9-A 到 D 是同一階段的工作順序，通過一段後繼續下一段；不把每份研究文件各自當成一個已完成的遊戲功能。R9 結束時須清楚列出已驗收的場次、招式與分支；其他站位變體／模式、launch 路徑仍有缺口時，不能把原清單第 1 項整項標為完成。

## 其餘工作的完成順序

| 階段 | 對應原清單 | 交付內容 | 結束時應能驗證 |
| --- | --- | --- | --- |
| R9 原作正常攻擊流程 | 第 1 項，沿用第 3 項基線 | 站位、目標、接近、發動、收招與拒絕條件的第一個完整正常案例；後續補齊各原始路徑 | 正常選招經原始條件發動，接回 R8，持續戰鬥至結算 |
| R10 投射物與完整演出 | 第 2 項 | 2D 資源池／世界參照、旋轉接觸、命中偏移、次級／落地效果、音效與鏡頭／停頓接線 | 先各做出近身、投射、範圍等經 ROM 確認的代表流程，再擴展；招式效果從生成到回收正確，不能全部代用兩種火花 |
| R11 異常狀態完整行為 | 第 3 項剩餘 | 補尚未完成的 AI state body 與進入、受擊中斷、期限解除、倒下／復起等交互條件 | 同樣條件得到相同可行動／不可行動結果、動畫與數值；逐條驗證原作是否存在該分支 |
| R12 自有角色組隊與數值接入 | 第 4 項前半 | 依原作編成資格與三個隊伍槽，使用牧場角色身分、目前養成數值和已取得招式；結算回到既有存檔權責 | 玩家以自己牧場的角色參賽；數值與技能來源可追溯，返回與 Save/Continue 不丟資料、不生成另一套角色 |
| R13 養成／狩獵／捕獲／進化動作 | 第 4 項後半 | 各玩法依原作條件觸發正確動作與數值寫入；沿用已研究或已完成的部分 | 每個玩法至少先完成一條正常可玩流程，再補原作各分支；素材可播放不等於玩法已接通 |
| R14 全角色逐招與裝置／出貨驗收 | 第 5 項，及前述未覆蓋變體 | 補齊每個角色／招式／模式的證據與正常路徑矩陣，實體手機測試，獨立核對素材／rights／shipping 狀態 | 每筆驗收能連到實際結果；正常玩法、視覺、裝置與 shipping 各自有證據，不用靜態呼叫覆蓋率代替 |

回歸與瀏覽器檢查每階段執行；實體裝置有可測版本時就開始，R14 再完成全範圍驗收。素材來源／可用範圍也在各階段持續核對，不能等全部完成才發現無法採用。這些工作沒有預設改變既有權利或 shipping 狀態。

## 驗收與阻塞的處理

每一個招式／流程分別記錄：原作來源、條件與數值實作、正常入口接通、畫面與動畫、裝置驗收、素材與 shipping。只有完整通過正常招式路徑的項目才增加 `normalGameplayAcceptedRecords`；裝置與 shipping 是另外的驗收，不能互相替代。

本輪已知待查／待接的事項：

- 初始站位寫入點、完整目標偏好及 launch 路徑仍須由原始執行確認。Evidence MCP 查詢 `02111F20` 未命中，只能依既有 R7 紀錄定位後再讀直接 ROM，不可把查無結果解讀為 ROM 沒有此功能。
- `0211DF68` 世界／2D 特效物件參照，以及 `0203EA30` 音效服務是 R10 已知入口；完整音訊資源對應與時序仍須查證。
- R12 的目前養成值／招式來源、原作出戰資格與結算後寫回條件，須逐欄核對現有 Raising／Save 合約，不自行套預設數值。
- 實體手機與 shipping 驗收尚未進行。本次沒有依此要求新增權限、購買工具或變更架構。

實作時遇到未知，只暫停依賴該資訊的分支，繼續已確認的獨立工作；保留明確阻塞原因與下一個查證位置，不捏造成功結果。

## 本次核對來源

- `AGENTS.md`、`README.md`、`docs/coordination/OWNER_DIRECTION.md`、`docs/architecture/CHAMPIONSHIP_2026_ARCHITECTURE.md`。
- `docs/CURRENT_PRODUCT_STATUS.md`、`docs/coordination/CHAMPIONSHIP_DEPENDENCY_MATRIX.csv`、`docs/coordination/CHAMPIONSHIP_BLOCKER_LEDGER.csv`。後兩份有歷史 NOT_STARTED 記錄，與 R7/R8 衝突時，以較新的實作合約與目前程式為準。
- `docs/contracts/championship/battle-native-lifecycle.v1.json`、`battle-hit-recovery.v1.json`；R8 正常場次及實作報告。
- `reports/battle-r8-regression.log`、`reports/battle-r8-coverage.log`：既有 1195/1195 與 normalGameplayAcceptedRecords=0。
- Evidence MCP：`YDIJ_BATTLE_REVERSE_CLOSURE_STAGE5_DELTA_2026-08-24/BATTLE_HIT_CONTACT_TRACE_v3.md` 第 17–43 行，SHA-256 `be655b3e341bb841b29472fe12bae079d4099b8255831e0f6a00a93737ba660c`。只提供上游啟動／接觸與資源檢查的研究線索，不能當成每條分支已驗證。查詢中其他研究檔案的相同地址命中也不能自動視為同一函式。

本輪只新增此計畫並在目前狀態頁加入連結。驗證為文件差異與格式檢查；沒有新 gameplay 測試或新的完成數字。
