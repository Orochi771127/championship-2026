# Production asset boundary

Only runtime-loadable, registered product assets belong here. VS1 temporary presentation art is stored under `temporary/int-rh2/` and must not be interpreted as a final visual baseline. ROM, Nitro, decoded reference pixels, and unregistered third-party assets are forbidden.

