# Verification

`npm test` runs deterministic runtime, contract, migration-firewall, and asset-registry checks. `npm run test:browser` performs the real reload/continue flow and five mobile viewport checks.

